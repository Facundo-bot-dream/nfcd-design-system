Rounded pill chip labelling a category or theme — the only place the brand uses fully round corners.

```jsx
<Tag>Somática</Tag>
<Tag tone="sage">Naturaleza</Tag>
<Tag tone="ink">Presencial</Tag>
```

Tones: `wine` (default), `ink`, `sage`. Keep labels to one or two words.
