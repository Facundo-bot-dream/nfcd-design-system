Square status marker for formats and states — sharper than a Tag, used for metadata like PRESENCIAL / ONLINE / COMPLETO.

```jsx
<Badge>Presencial</Badge>
<Badge variant="solid" tone="wine">Completo</Badge>
```

`variant`: `soft` (default tinted) or `solid` (filled). `tone`: `wine` · `ink` · `sage`.
